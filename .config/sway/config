#Hexagon16's (aka mbaraa) Configuration

#KeyBindings:

# set mod key to super(windows):
set $mod Mod4

# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

### Output configuration

# set $mon0 "eDP-1"
set $mon1 "DP-7"
set $mon2 "HDMI-A-4"

output $mon1 bg "~/General/Groot/Wallpapers/Not Mustafar Cats.png" fill
output $mon2 bg "~/General/Groot/Wallpapers/CatsPK.png" fill
# external big screen
output $mon1 resolution 1920x1080@180Hz position 0 0
output $mon2 resolution 1920x1080 position 1920 0 transform 270

# enable external
bindcode $mod+34 exec swaymsg "output $mon1 enable" && swaymsg "output $mon2 enable" && swaymsg "output $mon0 disable" && swaymsg "output $mon1 resolution 1920x1080@180Hz position 0 0" && swaymsg "output $mon2 resolution 1920x1080 position 1920 0 orientation 270"
# enable internal
bindcode $mod+35 exec swaymsg "output $mon1 disable" && swaymsg "output $mon2 disable" && swaymsg "output $mon0 enable" && swaymsg "output $mon0 resolution 1920x1200 position 0 0"

### Idle configuration
exec swayidle -w \
         timeout 300 '$HOME/.config/sway/lock.sh' \
         timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
         before-sleep '$HOME/.config/sway/lock.sh'

### Input configuration
input type:touchpad {
    dwt enabled
    tap enabled
    natural_scroll disabled
    middle_emulation enabled
}

input type:keyboard {
    xkb_layout "us,sy"
    xkb_options "grp:alt_shift_toggle"
}

input type:keyboard xkb_model "pc101"

input 1386:20639:Wacom_Pen_and_multitouch_sensor_Finger map_to_output eDP-1

input 1386:20639:Wacom_Pen_and_multitouch_sensor_Pen {
    tool_mode pen absolute
    map_to_output eDP-1
}

# start a terminal
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+Shift+q kill

# start Rofi
bindsym $mod+d exec $HOME/.config/rofi/launchers/type-1/launcher.sh

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# Reload the configuration file
bindsym $mod+Shift+r reload

bindsym $mod+x exec "$HOME/.config/sway/lock.sh"

# Exit sway (logs you out of your Wayland session)
bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Drag floating windows by holding down $mod and left mouse button.
# Resize them with right mouse button + $mod.
# Despite the name, also works for non-floating windows.
# Change normal to inverse to use left mouse button for resizing and right
# mouse button for dragging.
floating_modifier $mod normal

#
# Scratchpad:
#

# Move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym $mod+minus scratchpad show

# screenshotting
bindsym --release shift+Print exec ~/.config/sway/screenshot.sh window
bindsym --release ctrl+Print exec ~/.config/sway/screenshot.sh region
bindsym --release Print exec ~/.config/sway/screenshot.sh full

# Special keys to adjust volume via PulseAudio
bindsym --locked XF86AudioMute exec pactl set-sink-mute \@DEFAULT_SINK@ toggle
bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume \@DEFAULT_SINK@ -5%
bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume \@DEFAULT_SINK@ +5%
bindsym --locked XF86AudioMicMute exec pactl set-source-mute \@DEFAULT_SOURCE@ toggle
bindsym --locked XF86AudioPlay exec playerctl -a play-pause
bindsym --locked XF86AudioPause exec playerctl -a pause
bindsym --locked XF86AudioStop exec playerctl -a stop
bindsym --locked XF86AudioNext exec playerctl -a next
bindsym --locked XF86AudioPrev exec playerctl -a previous
# Special keys to adjust brightness via brightnessctl
bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+

# bucklespring keyboard sound
# bindsym $mod+b exec buckle

#Look & Feel
#Font Settings:
font pango:Terminess Nerd Font 9

######################################
# color settings for bar and windows #
######################################

# border_radius 16
# ceeriously styled i3
set $bg     #2f343f
set $fg     #404552
set $hi     #fce94f
set $ac     #4084d6
set $tx     #2d323d
set $ia     #9e9e9e
set $be     #8faf9f
set $yw     #f8f893
set $gn     #81c181
set $rd     #bf9b76
set $id     #f9f8ff
set $ce     #ceecee
set $sl     #7f7f7f

# set some nice colors      border  background  text    indicator
client.focused              $ac     $ac         $tx     $id
client.unfocused            $bg     $bg         $ia     $id
client.focused_inactive     $bg     $bg         $ac     $id
client.urgent               $ac     $ac         $tx     $id
#border & indicator are ignored
client.placeholder          $bg     $bg         $bg     $bg

##################
# Popups control #
##################
#Popups during fullscreen mode
popup_during_fullscreen smart

############
#Workspaces
############
#Workspaces Variables
set $ws1 "1: "
set $ws2 "2: "
set $ws3 "3: "
set $ws4 "4: "
set $ws5 "5:󰻧 "
set $ws6 "6:󰀂 "
set $ws7 "7: "
set $ws8 "8: "
set $ws9 "9:󱥊 "
set $ws10 "10: "
set $ws11 "11: "
set $ws12 "12: "

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
bindsym $mod+p workspace $ws11
bindsym $mod+o workspace $ws12

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+p move container to workspace $ws11
bindsym $mod+Shift+o move container to workspace $ws12

workspace $ws1 output $mon2
workspace $ws2 output $mon1
workspace $ws3 output $mon1
workspace $ws4 output $mon2
workspace $ws5 output $mon2
workspace $ws6 output $mon1
workspace $ws7 output $mon2
workspace $ws8 output $mon1
workspace $ws9 output $mon2
workspace $ws10 output $mon2
workspace $ws11 output $mon1
workspace $ws12 output $mon1

#Assign Programs to Workspaces
assign [class="vlc"] $ws7
assign [class="Firefox"] $ws3
assign [class="Thunar"] $ws4
assign [class="Xarchiver"] $ws4
assign [class="Mainwindow.py"] $ws12
assign [class="PPSSPPSDL"] $ws12
assign [class="qBittorrent"] $ws3
assign [class="Notepadqq"] $ws2
assign [class="Foxit Reader"] $ws9
assign [class="Steam"] $ws12
assign [class="Tuxcut.py"] $ws3
assign [class="VirtualBox Manager"] $ws11
assign [class="VirtualBox Machine"] $ws11
assign [class="Xfce4-display-settings"] $ws10
assign [class="Arandr"] $ws10
assign [class="Clementine"] $ws7
assign [class="Amarok"] $ws7
assign [class="GParted"] $ws10
assign [class="Gnome-disks"] $ws10
assign [class="i-nex.gambas"] $ws11
assign [class="Uget-gtk"] $ws3
assign [class="etcher-electron"] $ws11
assign [class="Google-chrome"] $ws3
assign [class="Nitrogen"] $ws10
assign [class="Lxappearance"] $ws10
assign [class="Wicd-client.py"] $ws3
assign [class="Brasero"] $ws8
assign [class="MEGAsync"] $ws3
assign [class="Nautilus"] $ws4
assign [class="Gnome-system-monitor"] $ws10
assign [class="rosettastoneversion3.exe"] $ws9
assign [class="Pavucontrol"] $ws10
assign [class="Blender"] $ws8
assign [class="Gimp"] $ws8
assign [class="libreoffice-writer"] $ws9
assign [class="libreoffice-draw"] $ws9
assign [class="libreoffice-math"] $ws9
assign [class="libreoffice-impress"] $ws9
assign [class="libreoffice-draw"] $ws9
assign [class="libreoffice-base"] $ws9
assign [class="libreoffice"] $ws9
assign [class="Opera"] $ws3
# assign [class="kitty"] $ws1
assign [class="stellarium"] $ws5
assign [class="GeoGebra"] $ws5
assign [class="kalzium"] $ws5
assign [class="gnome-genius"] $ws5
assign [class="Falkon"] $ws3
assign [class="Telegram"] $ws3
assign [class="Blueman-manager"] $ws10
assign [class="Vmware"] $ws11
assign [class="copyq"] $ws10
assign [class="Thunderbird"] $ws3
assign [class="processing-app-Base"] $ws2
assign [class="Designer"] $ws2
assign [class="Yandex-browser-beta"] $ws3
assign [class="Code"] $ws2
assign [class="okular"] $ws6
assign [class="corectrl"] $ws10
assign [class="Chromium"] $ws3
assign [class="DBeaver"] $ws2
assign [class="pulseaudio-equalizer.py"] $ws10
assign [class="jetbrains-goland"] $ws2
assign [class="Pulseaudio-equalizer-gtk"] $ws10
assign [class="jetbrains-idea"] $ws2
#assign [class=""] $ws
#assign [class=""] $ws
#assign [class=""] $ws

######################
#####  AutoStart  ####
######################
exec openrc --user graphical
exec --no-startup-id dbus-update-activation-environment --all
exec gentoo-pipewire-launcher restart &
# exec dbus-update-activation-environment DISPLAY I3SOCK SWAYSOCK WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
exec dbus-daemon --session --address=unix:path=$XDG_RUNTIME_DIR/bus
exec dbus-update-activation-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK DBUS_SESSION_BUS_ADDRESS XDG_CURRENT_DESKTOP=sway
exec export $(dbus-launch)
## # exec dbus-update-activation-environment DISPLAY I3SOCK SWAYSOCK WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
## # exec gentoo-pipewire-launcher restart &
## exec /usr/libexec/xdg-desktop-portal-wlr
## exec /usr/libexec/xdg-desktop-portal -r
exec /usr/lib64/xdg-desktop-portal -r
exec /usr/lib64/xdg-desktop-portal-wlr

## # remove on harvey
## # exec /usr/libexec/iio-sensor-proxy -v -r
## # exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK DBUS_SESSION_BUS_ADDRESS
## exec --no-startup-id xfce4-power-manager --restore # XfcePowerManager
## exec --no-startup-id kdeconnect-indicator --restore # KDE Connect
## exec kitty tty-clock -scx #Kitty hello
## # exec --no-startup-id /usr/libexec/polkit-mate-authentication-agent-1
## exec --no-startup-id /usr/lib/polkit-kde-authentication-agent-1
## # exec --no-startup-id /usr/lib64/libexec/polkit-kde-authentication-agent-1 # Polkit Agent
## exec --no-startup-id copyq  #CopyQ
## exec gnome-keyring-daemon --login
## exec /usr/bin/gnome-keyring --login # keyring
## exec udiskie
## exec waybar
## exec mako --default-timeout 3000

exec --no-startup-id xfce4-power-manager --restore # XfcePowerManager
exec --no-startup-id kdeconnect-indicator --restore # KDE Connect
# exec  kitty tty-clock -scx #Kitty hello
# exec --no-startup-id /usr/lib64/libexec/polkit-kde-authentication-agent-1 # Polkit Agent
exec rofi-polkit-agent -theme $HOME/.config/rofi/launchers/type-1/style-5.rasi
exec --no-startup-id copyq  #CopyQ
exec gnome-keyring-daemon --login
# exec /usr/bin/gnome-keyring --login # keyring
# exec --no-startup-id ~/.local/bin/eDP_ONLY.sh # nerfed screen resolution
exec udiskie
exec waybar
exec mako --default-timeout 3000
# exec dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK DBUS_SESSION_BUS_ADDRESS
# exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK DBUS_SESSION_BUS_ADDRESS

#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

for_window [class="^.*"] border pixel 0
gaps inner 5
gaps outer 5

####### FX
# --- Window Blurring ---
# This enables the dual-kawase blur algorithm
blur on
blur_xray off
blur_passes 2
blur_radius 5

# --- Corner Rounding (Optional but looks great with blur) ---
corner_radius 10

# --- Shadows ---
shadows on
shadow_blur_radius 15
shadow_color #0000007F

# Make inactive windows slightly transparent to show off the blur
for_window [class=".*"] opacity 0.95
default_dim_inactive 0.1
